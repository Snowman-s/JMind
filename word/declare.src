※変数宣言や、文字列変数宣言などに対応します。
※しかし、今は変数宣言のみに対応させます。
（文字列ー＞インデックス・真）
（文字列ー＞偽）
宣言単語？とは
    右端文字が　'は'に　等しいを　返すこと。

（識別子ー＞・）
単語認識宣言単語時の処理とは、
    識別子は　文字列
    識別子に　入れ

    関数定義文字列ファイルより一単語読み出し
    「変数」に　等しい文字列　でなければ
        "「～は」に対応する「変数」が有りません"で
        コンパイルエラーにして
        関数定義文字列ファイル一単語巻き戻し
        終わり
    つぎに
    
    宣言変数数を　一つ増加させ
    宣言変数名（宣言変数数）に　識別子を　入れ

    メソッドローカル変数を一つ枠取り　
    宣言変数インデックス（宣言変数数）に　入れ
    Integerのスタックマップテーブルデータを得て
    ローカル変数のバイナリを文字列追加し。